<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:System="using:System"
             mc:Ignorable="d"
             x:Class="DrinkPartyBillSplit.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="宴会名:" FontSize="Medium" />
        <Label Text="{Binding Item.Name}" d:Text="宴会名" FontSize="Small"/>
        <Label Text="開催日付:" FontSize="Medium" />
        <Label Text="{Binding Item.Date, StringFormat='{0:yyyy/MM/dd}'}" d:Text="開催日付" FontSize="Small"/>
        <Label Text="合計金額:" FontSize="Medium" />
        <Label Text="{Binding Item.TotalFee, StringFormat='{0:N0} 円'}" d:Text="合計金額" FontSize="Small" />
    </StackLayout>
    
</ContentPage>