<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DrinkPartyBillSplit.Views.NewItemPage"
             Title="New Item">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="キャンセル" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="保存" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="宴会名" FontSize="Medium" />
            <Entry Text="{Binding Item.Name}" Placeholder="(宴会名を入力)" FontSize="Small" />
            <Label Text="日付" FontSize="Medium" />
            <DatePicker Date="{Binding Item.Date}" />
            <Label Text="合計金額" FontSize="Medium" />
            <Entry Text="{Binding Item.TotalFee}" Placeholder="(合計金額を入力)" Keyboard="Numeric" FontSize="Small" />
            <!--<Editor Text="{Binding Item.Description}" d:Text="Item description" FontSize="Small" Margin="0" />-->
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
